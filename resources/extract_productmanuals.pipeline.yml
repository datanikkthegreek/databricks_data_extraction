resources:
  pipelines:
    extract_productmanuals_etl:
      name: extract_productmanuals_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      channel: preview
      serverless: true
      root_path: "../src/DataExtractionETL"

      # Pipeline configuration: passed as parameters to SQL (use ${key} in SQL)
      configuration:
        table: ${var.table}
        volume: ${var.volume}

      libraries:
        - glob:
            include: ../src/DataExtractionETL/transformations/productmanuals/**

      environment:
        dependencies:
          - --editable ${workspace.file_path}
